DatabaseErrorResponse:
  properties:
    statusCode:
      type: number
      description: Server status error
    message:
      type: string
      description: Error message
    error:
      type: object
      description: Error generated at runtime, showed in development mode
  example:
    statusCode: 500
    message: 'Invalid query findOne with parameter __$$1'
